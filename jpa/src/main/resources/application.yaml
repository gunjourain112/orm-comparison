spring:
  application:
    name: jpa
  datasource:
    url: jdbc:postgresql://localhost:5432/orm_compare
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
